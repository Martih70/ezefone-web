<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 512 512">
  <!-- Full bleed background for maskable icon (no rounded corners — OS applies mask) -->
  <rect width="512" height="512" fill="#1a6b3c"/>
  <!-- White E letterform, slightly smaller to stay in safe zone -->
  <text
    x="256"
    y="330"
    font-family="Arial Black, Arial, sans-serif"
    font-size="270"
    font-weight="900"
    text-anchor="middle"
    fill="#ffffff"
  >E</text>
  <!-- Gold accent dot -->
  <circle cx="355" cy="360" r="46" fill="#f59e0b"/>
</svg>
